{
  "$schema": "https://openapi.vercel.sh/vercel.json",

  "_comment_crons": "Cron jobs disabled on Hobby plan. Uncomment after upgrading to Pro, or trigger via external service like cron-job.org / Upstash QStash hitting /api/cron/* with Authorization: Bearer $CRON_SECRET.",

  "_disabled_crons": [
    { "path": "/api/cron/monthly-fee-gen",     "schedule": "0 2 1 * *" },
    { "path": "/api/messaging/send-queued",    "schedule": "0 6 * * *" },
    { "path": "/api/cron/compute-risk-scores", "schedule": "0 3 * * 0" }
  ]
}
